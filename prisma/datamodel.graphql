type Story {
  id: ID! @unique
  profileId: ID @unique
  title: String!
  description: String 
  content: String
  characters: [Character!]!
  genre: Genre
  approved: Boolean @default(value: "false")
  isPublished: Boolean @default(value: "false")
  views: Int
  likes: Int
  clones: Int
  # submissionID: ID
}

type Character {
  id: ID! @unique
  name: String!
  age: Int
  backStory: String
}

type Block { 
  blockNumber: Int!
  blockContent: String!
  # submissionID: Submission
}

type Submission {
  id: ID! @unique
  flag: Boolean @default(value: "false")
  # blocks: [Block!]!
}

enum Genre {
  Tragedy
  TragicComedy
  Fantasy
  Mythology
  Adventure
  Mystery
  ScienceFiction
  Drama
  Romance
  ActionAdventure
  Satire
  Horror
}

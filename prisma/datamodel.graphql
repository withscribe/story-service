type Story {
  id: ID! @unique
  title: String!
  description: String! 
  approved: Boolean @default(value: "false")
  submissionNumber: Int
  author: String
}

type Block {
  submissionNumber: ID  @unique
  blockNumber: Int!
  blockContent: String!
}

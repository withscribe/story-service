type Token {
  symbol: String! @unique
  name: String! 
  abi: String!
  address: String! @unique
  totalSupply: Int
  price: Float
  holders: Int
  officialSite: String
  decimals: Int
  marketCap: Float
  circulatingSupply: Int
}

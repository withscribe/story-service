# import Story from './generated/prisma.graphql'
# import Block from './generated/prisma.graphql'


type Query {
  stories(searchString: String!): [Story!]!
  story(searchString: String!): Story
  # blocks(searchString: String!): [Block!]!
  # block(searchString: String!): Block
}

type Mutation {
  createStory(data: StoryCreateInput!): Story

  # createStory(title: String!, description: String!, author: String! ): Story
  #   createBlock(blockNumber: Int!, blockContent:String!): Block
  #  createBlock(blockNumber: Int!, blockContent:String!): Block
}

